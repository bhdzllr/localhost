{
  "name": "localhost",
  "version": "1.0.0",
  "description": "Local web server",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "bhdzllr",
  "license": "ISC",
  "private": "true",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-webserver": "^0.9.1"
  }
}
