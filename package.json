{
  "name": "localhost",
  "version": "2.0.0",
  "description": "Local web server",
  "private": true,
  "author": "bhdzllr",
  "license": "MIT",
  "scripts": {
    "start": "npm run serve",
    "serve": "eleventy-dev-server --input=www --port=3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@11ty/eleventy-dev-server": "^1.0.0-canary.10"
  }
}
